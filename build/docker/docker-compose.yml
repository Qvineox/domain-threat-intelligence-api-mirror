version: '3.9'

services:
  dti_db:
    container_name: dti_db
    hostname: database

  dti_api:
    container_name: dti_api
    depends_on:
      - dti_db
    environment:
      db_host: $DB_HOST
      db_port: $DB_PORT
      db_user: $DB_USER
      db_pass: $DB_PASS
      db_name: $DB_NAME